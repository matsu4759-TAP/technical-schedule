# 技術部スケジュール管理表 セットアップ手順

グループ内(9人)でアカウントなしに使える、技術部予定・デモ予定の共有ボードです。
スタッフ・使用装置・作業種類を管理でき、月表示/メンバー別表示の切り替えや、
装置の空き状況の確認、デモ予定の装置重複チェックもできます。
誰かが編集すると全員の画面にリアルタイムで反映されます。

セットアップは **あなた(管理者)が1回だけ** 行えば、あとはメンバーはURLを開くだけで使えます。

## 1. データの保存先(Supabase)を用意する

1. https://supabase.com にアクセスし、メールアドレスで無料アカウントを作成します(Googleログインは不要です)。
2. 「New project」から新しいプロジェクトを作成します。
   - 名前は自由(例: `group-schedule`)
   - リージョンは `Northeast Asia (Tokyo)` を推奨
   - データベースのパスワードは自動生成のままで構いません
3. プロジェクト作成後、左メニューの **SQL Editor** を開き、「New query」に
   このリポジトリの [`schema.sql`](./schema.sql) の内容をすべて貼り付けて **Run** を押します。
   - `schedule_events`(予定)と `schedule_config`(スタッフ・装置・作業種類の設定)という
     2つのテーブルが作成されます。
4. 左メニューの **Database → Replication** を開き、`schedule_events` と `schedule_config`
   の **両方** のテーブルでRealtimeを **ON** にします(これで編集内容が全員の画面に自動反映されます)。
5. 左メニューの **Project Settings → API** を開き、次の2つをコピーします。
   - **Project URL**
   - **anon public** キー

## 2. 合言葉を決める

メンバーはアカウントを持たないため、URLを知っていれば誰でも閲覧・編集できてしまいます。
その抑止として、ページを開く際に合言葉を入力させる簡易的な仕組みを用意しています。
(強固な認証ではなく、あくまで「URLが漏れたときの抑止」程度のものです。)

## 3. 設定ファイルを編集する

このリポジトリの [`config.js`](./config.js) を開き、以下を書き換えてコミット・プッシュします。

```js
window.SCHEDULE_CONFIG = {
  supabaseUrl: "手順1でコピーしたProject URL",
  supabaseAnonKey: "手順1でコピーしたanon publicキー",
  passcode: "好きな合言葉",
  groupName: "好きなカレンダーの名前",
};
```

> `supabaseAnonKey` はブラウザのソースから誰でも見える値です。これはSupabaseの想定通りの
> 仕様で、アクセス制御はデータベース側のポリシー(`schema.sql` で設定済み)で行っています。
> 機密情報を書き込む用途には使わないでください。

## 4. GitHub Pages を有効にする

1. GitHubのリポジトリで **Settings → Pages** を開きます。
2. **Source** を「Deploy from a branch」にし、ブランチは `main`、フォルダは **`/ (root)`** を選択して **Save** します。
3. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` のようなURLが発行されます。

## 5. メンバーに共有する

発行されたURLと合言葉を、9人のグループメンバーに共有してください。
メンバーはアカウント登録不要で、URLを開いて合言葉を入力するだけで使えます。

---

## セキュリティについて

- このページは「URLと合言葉を知っている人なら誰でも閲覧・追加・編集・削除できる」設計です。
  アカウントによる本人確認は行っていません。
- 検索エンジンにインデックスされないよう `noindex` を設定していますが、リンクを知っていれば
  誰でもアクセスできる点は変わりません。
- 機密性の高い情報(個人情報など)は登録しないでください。
